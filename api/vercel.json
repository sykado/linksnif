{
  "version": 2,
  "builds": [
    { "src": "public/**", "use": "@vercel/static" },
    { "src": "api/**", "use": "@vercel/node" }
  ],
  "routes": [
    { "src": "/api/(.*)", "dest": "api/$1" },
    { "src": "/(.*)", "dest": "public/$1" }
  ]
}
# Add this to package.json
"dependencies": {
  "yt-dlp-exec": "^1.0.0"
}